developer:
  name: John Jayson A. Lopez
  contact:
    url: 'https://www.linkedin.com/in/johnjaysonlpz'
    email-address: johnjaysonlopez28@gmail.com
server:
  port: 8000
  contextPath: /api/v1/authorization-server
security:
  oauth2:
    client:
      id: none
      client-secret: none
      resource-ids: none
      access-token-uri: none
spring:
  application:
    name: '@project.name@'
    version: '@project.version@'
    description: '@project.description@'
  datasource:
    url: 'jdbc:postgresql://172.17.0.1:5432/postgres'
    username: postgres
    password: postgres
    platform: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: public
